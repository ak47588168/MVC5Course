@model double?

@if (Model.HasValue)
{
    <span style="font-family:'Apple Color Emoji' , 'Segoe UI Emoji' , 'NotoColorEmoji' , 'Segoe UI Symbol' , 'Android Emoji' , 'EmojiSymbols'">
        @for (int i = 0; i < Model.Value; i++)
        {
            @:⭐
        }
    </span>
}